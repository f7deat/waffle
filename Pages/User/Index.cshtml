@page
@model Waffle.Pages.User.IndexModel

<div class="container">
    <div class="w-row">
        <div class="w-1/3">
            <div class="card">
                <div class="flex justify-center mb-4">
                    <img src="@Model.Avatar" loading="lazy" alt="Avatar" class="w-40 h-40 rounded-full" />
                </div>
                <div class="text-xl font-bold mb-4">@Model.IdentityUser.UserName</div>
            </div>
        </div>
        <div class="w-2/3">
            <div class="card"></div>
        </div>
    </div>
</div>