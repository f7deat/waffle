@{
    var css = ViewBag.CSS as IEnumerable<Guid>;
}

@if (css != null)
{
    @foreach (var item in css)
    {
        <link rel="stylesheet" href="@($"/css/{item}.css")" asp-append-version="true" />
    }
}