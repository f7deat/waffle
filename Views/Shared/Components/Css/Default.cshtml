@model IEnumerable<Waffle.Entities.WorkContent>

@if (Model != null && Model.Any())
{
    @foreach (var item in Model)
    {
        <link rel="stylesheet" href="/css/@($"{item.Id}.css")" asp-append-version="true" />
    }
}