@using Waffle.Models.Components
@model Feed

<div class="wf-feed">
    <div class="container">
        <div class="title">@Model.Name</div>
        @if (Model.Articles != null)
        {
            <partial name="~/Pages/Article/_ListItem.cshtml" model="Model.Articles" />
        }
    </div>
</div>