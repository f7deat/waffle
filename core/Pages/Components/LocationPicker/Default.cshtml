@using Waffle.Extensions;
@using Waffle.Models.Components.Pickers
@model LocationPicker

<div class="article-picker mb-4">
    <div class="title">
        <div>@Model.Title</div>
        <a href="/location" class="text-body" aria-label="View More">
            <i class="fa-solid fa-angles-right"></i>
        </a>
    </div>
    <div class="bg-white shadow rounded">
        @for (var i = 0; i < Model.Locations.Count; i++)
        {
            if (i == 0)
            {
                <div>
                    <a href="@Model.Locations[i].Url" class="thumbnail">
                        <img src="@Model.Locations[i].Thumbnail" alt="@Model.Locations[i].Name" loading="lazy" class="w-full object-cover" height="250" />
                    </a>
                    <div class="px-2 py-1">
                        <div class="mb-1 fw-medium text-truncate"><a href="@Model.Locations[i].Url">@Html.Raw(Model.Locations[i].Name)</a></div>
                        <div class="text-muted line-clampin">
                            @Model.Locations[i].Description
                        </div>
                    </div>
                </div>
                continue;
            }
            <div class="border-top px-2 py-1">
                <div class="mb-1 fw-medium text-truncate"><a href="@Model.Locations[i].Url">@Html.Raw(Model.Locations[i].Name)</a></div>
                <div class="text-muted text-sm flex">
                    <div class="me-2"><i class="fa fa-calendar me-2"></i>@Model.Locations[i].ModifiedDate</div>
                    <div><i class="fas fa-eye me-2"></i>@Model.Locations[i].ViewCount.ToNumber()</div>
                </div>
            </div>
        }
    </div>
</div>